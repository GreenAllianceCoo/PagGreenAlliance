# Buzón del supervisor de avances

Cada agente `ga-*` agrega aquí un reporte al terminar una tarea importante. `ga-supervisor-avances` los pasa a `plan.json`, regenera `Avances_Green_Alliance.xlsx` y vacía este buzón (lo procesado queda en la hoja Bitácora).

Formato (copiar debajo de la línea, uno por tarea):

```
## AAAA-MM-DD · nombre-del-agente
- Actividades: 2.5, 2.6            (IDs de la hoja Plan; P-xx si es un pendiente suelto)
- Estado: Hecho | En curso | Bloqueado
- Qué se hizo: una o dos frases.
- Bloqueos o trabajo nuevo: qué falta, de quién, o «ninguno».
```

---

## 2026-09-23 · ga-verificador-responsive
- Actividades: 6.3
- Estado: Hecho
- Qué se hizo: Revisé responsive 8 rutas × 9 anchos (360–1440), más nombre largo y 844×390, con Playwright en local (develop bf09975). 0 críticos y 8 menores: sin scroll horizontal y con los patrones de la spec correctos. Reporte y capturas en docs/verificaciones/responsive-2026-09-23/.
- Bloqueos o trabajo nuevo: ga-diseno-a-codigo debe corregir 8 hallazgos menores. Los más visibles: el texto de las opciones 50/100 % en /cuenta/solicitar en celular, y el área de toque de «Reenviar código» y del deslizador de monto. También: header de /cuenta con nombres largos a 1024, hero de la landing a 1024, landing estirada entre 640 y 1023, /afiliacion a 1024 y hueco en /afiliacion/enviada en tableta.

## 2026-09-23 · ga-verificador-qa
- Actividades: 6.3, P-39
- Estado: Bloqueado
- Qué se hizo: Verificación E2E del commit bf09975 en local (Supabase local + next dev): 236 pruebas Playwright pasan y 5 fallan por hallazgos reales; unitarias (140), pgTAP (143), tsc y eslint sin errores. Nuevo tests/e2e/j-produccion.spec.ts y ajustes en D5, H, I3, I4, I6 y Z. Reporte: docs/verificaciones/2026-09-23-verificacion-3.md.
- Bloqueos o trabajo nuevo: NO APROBADO para producción. F-01 crítica: el dominio del correo enmascarado delata si la cédula existe (ga-funcionalidad-botones + decisión de producto). F-02 alta: sin tope de intentos al verificar el código (ga-funcionalidad-botones, revisar con ga-revisor-seguridad). Contenido pendiente de la cooperativa visible en la landing y política de datos vacía (Ley 1581). Configuración de producción por revisar: SMTP/Resend, P-45, secretos en Vercel, P-43 y P-09. Además, F-03 (celular sin «Cerrar sesión» en /cuenta/solicitar) y F-04 (404 en inglés) para ga-diseno-a-codigo; F-05 (sin cabeceras anti-clickjacking) para ga-funcionalidad-botones; F-06 a F-08 bajas.

## 2026-09-23 · ga-funcionalidad-botones
- Actividades: 6.3, P-49
- Estado: Hecho
- Qué se hizo: Corregí F-01 (correo enmascarado ya no delata la cédula: se oculta también el dominio y el relleno se genera con la misma mezcla de dominios institucionales reales), F-02 (tope de intentos al verificar el código: 5 por cédula y 30 por IP cada 15 min, con `dentroDelLimite`), F-05 (cabeceras `X-Frame-Options`, CSP `frame-ancestors 'none'`, `Referrer-Policy` y `X-Content-Type-Options` en `next.config.mjs`), F-06 (el servidor de `/cuenta/solicitar` exige monto múltiplo de $50.000) y F-08 (el código pegado durante «Reenviar» ya no se borra). Documenté O-01 en `lib/servidor/limite.ts` (x-forwarded-for/x-real-ip solo confiables detrás de Vercel). Agregué pruebas unitarias para F-01, F-02, F-06 y O-01 (tests/unit/cuenta-y-mascara.test.ts, ingreso-codigo.test.ts nuevo, crear-solicitud.test.ts, limite.test.ts). `tsc`, `eslint` y `test:unit` sin errores nuevos (162/163; la falla restante de `enlaceWhatsapp` es previa y no la toqué). No toqué `supabase/`, `components/` ni `app/afiliacion/`.
- Bloqueos o trabajo nuevo: `tests/e2e/j-produccion.spec.ts`, `i-despliegue.spec.ts` y `b-ingreso.spec.ts` (de ga-verificador-qa) usan el formato viejo de máscara y dominios gmail/hotmail/outlook/yahoo; hay que actualizarlos al nuevo formato `xx•••@yy•••.co`. `supabase/config.toml` → `[auth.rate_limit] token_verifications = 30` por IP cada 5 min: como nuestras llamadas a `verifyOtp` salen desde el servidor (Vercel), ese límite de Supabase se reparte entre TODOS los usuarios, no por persona; recomiendo subirlo bastante en el panel de producción (o no bajarlo del valor por defecto) y tratarlo solo como respaldo, ya que el límite real por cédula/IP ahora lo hace nuestra app. Pendiente decisión de la cooperativa: confirmar que solo hay 3 dominios institucionales de asociados (policia.gov.co, buzonejercito.mil.co, ejercito.mil.co) para no dejar ningún dominio real fuera de la mezcla de relleno.

## 2026-09-23 · ga-diseno-a-codigo
- Actividades: 6.3
- Estado: Hecho
- Qué se hizo: Corregí los 8 hallazgos de docs/verificaciones/responsive-2026-09-23/reporte.md y F-03/F-04/F-07 de la verificación 3, sin tocar Afiliacion.tsx, app/afiliacion/page.tsx, Cuenta.tsx, lib/, app/ingresar/codigo/ ni supabase/. Tarjetas 50%/100% de /cuenta/solicitar con padding y texto centrado; deslizador con área de toque de 44 px; «Reenviar código» con min-h-11; header de /cuenta con nav sin partir (whitespace-nowrap) y nombre truncado; «Cerrar sesión» agregado en celular a /cuenta/solicitar; botones del hero y «Ver beneficios» de la landing sin partirse entre 1024–1150 y landing centrada (md:max-w-2xl) entre 640–1023, igual que /afiliacion y /cuenta; hueco de tableta en /afiliacion/enviada corregido (md:min-h-0); nueva app/not-found.tsx en español con logo y «Volver al inicio». Verifiqué con tsc, eslint, npx playwright test -c tests/responsive (19/21, 2 fallas ya documentadas como falsos positivos) y capturas manuales a 360/390/768/1024/1150/1280.
- Bloqueos o trabajo nuevo: ninguno de mi parte. El hallazgo #7 (afiliacion-1024, grid a 2 columnas muy angosta) queda pendiente para la tanda que rehaga Afiliacion.tsx, según lo pedido.

## 2026-09-24 · ga-auditor-supabase
- Actividades: Fase 2 (nueva, sin ID en plan.json todavía) — PARTE 1: base de datos y spec. Relacionada con 1.2, 3.1.
- Estado: Hecho
- Qué se hizo: 6 migraciones nuevas (no aplicadas a producción, solo probadas contra Supabase local): rol 'asesor' (enum aparte por la restricción de Postgres de no usar el valor nuevo en la misma transacción), `perfiles.asesor_id` con validaciones (debe ser un asesor real, no autoreferencia, nadie se autoasigna asesor ni rol), `solicitudes_afiliacion` v2 (nombres/apellidos separados, institución + correo institucional según policía/ejército, Nequi, asesor_id opcional, 3 fotos obligatorias; `unidad` queda nullable sin borrarse), `obtener_asesores_publico()` y `resumen_clientes_asesor()` (SECURITY DEFINER, columna a columna, nunca celular/correo/nequi/fotos), bucket privado `afiliacion-documentos` (5 MB, jpeg/png/webp, sin ninguna política de storage.objects: todo por service_role + URL firmada desde el servidor tras verificar admin), y sorteo mensual (`boletas_sorteo` + `participar_sorteo()`/`confirmar_boleta_sorteo()`/`sorteo_ventana_abierta()`, ventana 1–5 hora Bogotá, una boleta por asociado y mes, tope de 5 intentos). `supabase/seed.sql` actualizado (asesor de prueba cédula 1234567892 asignado al asociado 1234567890, solicitud de afiliación de ejemplo con institución y Nequi). 4 archivos de pruebas pgTAP nuevos (07 a 10) y 2 ajustados (00, 04) por el cambio de esquema. `docs/spec-fase-2.md` nuevo con tablas, funciones, cómo llamarlas desde el servidor y preguntas abiertas. Verificado con `supabase db reset` desde cero + `supabase test db` (203/203 pgTAP) + `npm run test:unit` (163/163) + `tsc --noEmit` sin errores. No toqué `lib/ingreso/`, `next.config.mjs` ni componentes visuales.
- Bloqueos o trabajo nuevo: Sebas debe aplicar estas 6 migraciones a producción cuando corresponda (después de la auditoría de seguridad de esta tanda), igual que la pendiente ya existente de `limite_de_intentos`. `ga-funcionalidad-botones` necesita: (1) sumar a `docs/resend-plantillas.md` el correo del número de boleta del sorteo (no estaba en la lista de 3 correos existente); (2) construir el formulario de afiliación nuevo (nombres/apellidos, institución, Nequi, asesor, 3 fotos) y la subida al bucket privado con service role; (3) la pantalla del asesor (resumen_clientes_asesor) y su cuenta de demostración (decidida por la cooperativa como solo-app, sin tablas); (4) el flujo de participar/confirmar del sorteo. `ga-diseno-a-codigo` necesita maquetar esas pantallas nuevas (no existen en `docs/mapa-de-botones.md` todavía). Preguntas abiertas para la cooperativa en `docs/spec-fase-2.md` §"Preguntas abiertas" (plantilla del correo de boleta, tope de intentos del sorteo, cómo se activa la demo del asesor, texto del asesor en el desplegable público, import de asesores en lote).

## 2026-09-24 · ga-funcionalidad-botones
- Actividades: Fase 2 · Pantalla del asesor y su cuenta de demostración (PARTE 2C de spec-fase-2.md; sin ID propio en plan.json todavía)
- Estado: Hecho
- Qué se hizo: `/asesor` («Mis clientes», con buscador y filtro por estado sobre `resumen_clientes_asesor()`) y `/asesor/demo` (cuenta fantasma que no escribe nada, con selector de grado y «Nueva solicitud» simulada), ambas con guardia de rol propia en el servidor; 26 pruebas unitarias nuevas (`tests/unit/asesor-resumen.test.ts`, `tests/unit/asesor-demo.test.ts`) y `docs/mapa-de-botones.md` actualizado con las secciones 7 y 7b.
- Bloqueos o trabajo nuevo: ninguno de mi parte. Dos cosas a coordinar con otros agentes: (1) el agente de `proxy.ts`/`app/ingresar/actions.ts` debe agregar `/asesor` al matcher y redirigir por rol tras el login (hoy un asesor que ingresa cae en `/cuenta`, no en `/asesor`; mi página igual protege por su cuenta si se navega directo a `/asesor`); (2) `npx tsc --noEmit`, `npm run lint` y `npm run test:unit` muestran errores/fallas preexistentes fuera de mi zona (`app/admin/afiliaciones/[id]/page.tsx`, `tests/unit/ingreso-codigo.test.ts`, `tests/unit/validaciones.test.ts`) por trabajo en curso de otros agentes sobre afiliación v2/admin; no los toqué. Verifiqué el flujo real del asesor de prueba (cédula 1234567892) contra Supabase local con un script temporal (login + `resumen_clientes_asesor()`): ve correctamente al Asociado de Prueba (1234567890, crédito pendiente) y a la solicitud de afiliación referida (1234567899); el script se borró después, no quedó en el repo.

## 2026-09-24 · ga-funcionalidad-botones
- Actividades: Fase 2 · Sorteo mensual en /cuenta (PARTE 2D de spec-fase-2.md §4; sin ID propio en plan.json todavía)
- Estado: Hecho
- Qué se hizo: botón «Sorteo del mes» (destacado y animado del 1 al 5 de cada mes, hora de Colombia; fuera de esa ventana muestra «Próximo sorteo: 1 de <mes>», salvo que el asociado ya haya confirmado su boleta ese mes) y su modal accesible en `/cuenta` (`components/sorteo/SorteoDelMes.tsx`, autocontenido: no hizo falta tocar `CuentaCliente.tsx`). Los 3 pasos de la spec: «Quiero participar» (Server Action `participarSorteo` → RPC `participar_sorteo()` → correo con el número por Resend, `lib/correo/sorteo.ts`, plantilla nueva `ga-sorteo-boleta` documentada en `docs/resend-plantillas.md` §4; sin plantilla configurada, el número queda solo en el log del servidor para poder probar en local), confirmar (Server Action `confirmarBoletaSorteo` → RPC `confirmar_boleta_sorteo()`, con intentos restantes y tope de 5 sin decir «intentos agotados») y celebración (confeti CSS en verde/dorado de la marca + boleta que entra girando + número dígito por dígito, todo en `components/sorteo/confeti.module.css` y `components/sorteo/confeti.ts`, que respeta `prefers-reduced-motion`). El número de boleta nunca sale del servidor antes de confirmar: `lib/sorteo/vista.ts#vistaSorteo` lo oculta de las props del cliente aunque la fila ya lo tenga (estado `enviada`). 31 pruebas unitarias nuevas (`tests/unit/sorteo.test.ts`, `tests/unit/sorteo-acciones.test.ts`): fechas en hora de Bogotá, qué expone `vistaSorteo`, confeti sin `prefers-reduced-motion`, y las Server Actions rechazando sin sesión. `docs/mapa-de-botones.md` (§4) y `.env.example` (`RESEND_TEMPLATE_SORTEO_BOLETA`) actualizados. `tsc --noEmit`, `eslint` y `npm run build` sin errores nuevos; `npm run test:unit` sin fallas nuevas (las 8 que fallan son previas, de `validaciones.test.ts`/`ingreso-codigo.test.ts` por trabajo en curso de afiliación v2, fuera de mi zona). No toqué `/admin`, `/asesor`, `/afiliacion` ni `proxy.ts`.
- Bloqueos o trabajo nuevo: `participar_sorteo()`/`confirmar_boleta_sorteo()` usan `now()` sin parámetro de fecha para pruebas, así que hoy (24-sep, fuera de la ventana 1–5) no pude probar el flujo feliz de extremo a extremo contra la base real; quedó cubierto con pruebas unitarias (mocks) y revisión de código. Para probarlo de verdad hay dos opciones: (a) esperar al 1–5 del próximo mes con la cédula de prueba 1234567891, o (b) un agente con acceso a la base podría, solo en local, insertar manualmente una fila de prueba en `boletas_sorteo` (sin pasar por la función) para ver los pasos de lectura/confirmación de la interfaz — no lo hice yo para no tocar datos fuera de mi zona de código. Pendiente de la cooperativa (ya reportado por `ga-auditor-supabase`): texto final y aprobación de la plantilla `ga-sorteo-boleta` en Resend, y confirmar si el tope de 5 intentos les sirve. Para `ga-diseno-a-codigo`: agregué el botón del sorteo dentro del `<nav>` de accesos rápidos de `/cuenta` (no hay maqueta para esta pantalla); agradecería una revisión de ubicación, copy e ícono cuando se pueda.

## 2026-09-24 · ga-auditor-supabase
- Actividades: Fase 2 (sin ID en plan.json todavía): auditoría de las 6 migraciones 20260924000000…000500 antes de producción. Relacionada con 6.5.
- Estado: Hecho
- Qué se hizo: Audité las 6 migraciones de la fase 2 con el repo y Supabase local, sin el MCP y solo en lectura (203/203 pgTAP). No hay nada crítico. Hay 1 hallazgo alto: el asociado puede leer el número de su boleta del sorteo antes de confirmarlo por la API REST. Propuse 3 migraciones nuevas sin aplicar: 000600 oculta el número y corrige las carreras del sorteo, 000700 hace que el asesor degradado deje de ver a sus clientes, 000800 agrega un trigger que sincroniza la cédula y el grado desde app_metadata. Reporte en docs/auditorias/2026-09-24-fase-2.md.
- Bloqueos o trabajo nuevo: ga-funcionalidad-botones debe pasar a RPC app/cuenta/page.tsx, app/cuenta/actions-sorteo.ts y app/admin/sorteo/page.tsx (mi_boleta_sorteo, boletas_confirmadas_sorteo) y agregar «Reenviar mi boleta». ga-escritor-tests debe ajustar 10_sorteo_mensual.sql y agregar las pruebas de §5 del reporte. Sebas debe aplicar 000000…000800 con db push, después de comprobar que hay 0 afiliaciones en producción, y desplegar enseguida. La cooperativa tiene 7 preguntas abiertas (grados del Ejército, dominios, retención de fotos, etc.).

## 2026-09-24 · ga-revisor-seguridad
- Actividades: 6.2 (revisión de seguridad de la fase 2, diff 4545c0e..235ffbe: /admin, /asesor, /afiliacion v2, sorteo, proxy, ingreso por rol, cabeceras, llaves)
- Estado: En curso
- Qué se hizo: Revisé el código nuevo de la fase 2. No hay secretos expuestos, ni open redirect, ni bypass de rol. /admin y /asesor verifican el rol en el servidor antes de usar service role. Bloquean el despliegue: (1) el asociado puede leer el número de su boleta por la API REST (F2-01, ya reportado por ga-auditor-supabase); (2) la autorización de datos sensibles enlaza a una política de datos que todavía es un placeholder; (3) el límite de 1 MB de las Server Actions tumba el envío de las 3 fotos (es un problema funcional).
- Bloqueos o trabajo nuevo: ga-funcionalidad-botones: validar las fotos por magic bytes, sacar el número de boleta de los logs, no revelar si una cédula tiene una afiliación pendiente, filtrar por estado 'pendiente' en el update de créditos, configurar serverActions.bodySizeLimit o subir las fotos directo con URL firmada de subida, y quitar la dependencia `workflow`, que no se usa y tiene vulnerabilidades altas. ga-auditor-supabase: aplicar 000600/000700/000800. Sebas y la cooperativa: texto real de la política de datos y de la autorización de datos sensibles (Ley 1581), y decidir si admin o asesor pueden participar en el sorteo.

## 2026-09-24 · ga-funcionalidad-botones
- Actividades: 6.2, 6.5 (correcciones de la auditoría docs/auditorias/2026-09-24-fase-2.md F2-01/F2-04 y de la revisión de seguridad S-01 a S-15 del bloque anterior de ga-revisor-seguridad; sin ID propio en plan.json todavía)
- Estado: Hecho
- Qué se hizo: Rama develop, sin tocar producción. (1) F2-01/S-01: apliqué en local 000600/000700/000800 (`supabase db reset`) y pasé a RPC `app/cuenta/page.tsx`, `app/cuenta/actions-sorteo.ts` y `app/admin/sorteo/page.tsx` (`mi_boleta_sorteo`, `boletas_confirmadas_sorteo`); ajusté `supabase/tests/00_rls_y_privilegios.sql` (excepciones nuevas) y `10_sorteo_mensual.sql`, y agregué `supabase/tests/11_sincronizar_perfil_app_metadata.sql` (trigger 0800) — `npm run test:db` en verde (222/222). (2) F2-04: «Reenviar mi boleta» en el modal (mismo número, sin generar uno nuevo, tope 3/día con `dentroDelLimite`); el paso «confirmar» ahora se abre directo si la boleta ya está «enviada». (3) S-03: `CampoFoto.tsx` comprime SIEMPRE a JPEG ≤1600px apuntando a ~300 KB, y `next.config.mjs` con `experimental.serverActions.bodySizeLimit: "3mb"`. (4) S-04: `detectarTipoImagen` (magic bytes JPEG/PNG/WEBP) en `lib/afiliacion/fotos.ts`, sube con el tipo detectado (nunca `archivo.type`); pruebas en `tests/unit/afiliacion-fotos.test.ts`. (5) S-05: el número de boleta solo se registra en el log fuera de producción (`lib/correo/sorteo.ts`); `next.config.mjs` exige en Vercel `RESEND_API_KEY`, `EMAIL_FROM` y `RESEND_TEMPLATE_SORTEO_BOLETA` además de `LIMITE_HMAC_SECRET`. (6) S-10: `npm uninstall workflow` (no se usaba) — `npm audit --omit=dev` da 0 vulnerabilidades (quedan 2 moderadas, solo en `vitest`/dev, piden un salto de versión mayor que no hice). (7) S-11: el update de `solicitudes_credito` ahora filtra también por `estado = 'pendiente'` y el correo solo se envía si `.select().maybeSingle()` confirma una fila actualizada. (8) S-12: `SITIO_URL` nueva en `.env.example`; el correo «Ingreso aceptado» la usa primero y cae al host de la petición solo si falta (typ. desarrollo). (9) S-13: `participarSorteo` exige `rol = 'asociado'` en el servidor y `/cuenta` oculta el botón para cualquier otro rol. (10) S-14: `Strict-Transport-Security` y `Permissions-Policy` en `next.config.mjs`, sin tocar la CSP existente (no agregué `default-src`). (11) Vista previa `/cuenta?sorteo=demo` (`lib/sorteo/demo.ts` + `SorteoDelMes.tsx`): simula los 3 pasos con el código fijo 123456, nunca toca la base ni Resend, y SOLO funciona si `NODE_ENV !== "production"` (prueba unitaria en `tests/unit/sorteo-demo.test.ts`). (12) S-06: `app/afiliacion/actions.ts` ya no tiene una comprobación previa de cédula duplicada; ahora sigue el mismo camino que un envío exitoso (sube fotos, intenta el insert) y, si choca el índice único parcial (23505), responde EXACTAMENTE igual que un envío exitoso (mismo flash, mismo redirect), borra las fotos ya subidas y solo registra `afiliacion_duplicada` sin datos personales; actualicé `docs/mapa-de-botones.md`, `docs/spec-afiliacion-y-login.md`, `tests/e2e/d-afiliacion.spec.ts` (D6) y agregué `tests/unit/afiliacion-duplicada.test.ts`. (13) S-07: recuadro de advertencia antes de Aprobar/Rechazar en `/admin/afiliaciones/[id]` con el texto pedido, y el correo institucional repetido junto a las fotos; sin paso de verificación por código. Verificación: `npx tsc --noEmit`, `npm run lint`, `npm run build` y `npm run test:unit` (307/307) sin errores; `npx playwright test -c tests/e2e k-roles --project=escritorio` sigue en verde (4/4).
- Bloqueos o trabajo nuevo: `tests/e2e/d-afiliacion.spec.ts` (todo el archivo, no solo D6) ya estaba roto ANTES de esta tarea: usa el formulario viejo de afiliación (`getByLabel("Nombres y apellidos")`, campo `unidad`, sin institución/Nequi/asesor/fotos), que quedó reemplazado por la afiliación v2 de la tanda anterior; lo confirmé corriendo D6 solo (falla en el primer `llenarAfiliacion`, antes de llegar a mi cambio) — hace falta que alguien reescriba ese archivo entero para el formulario v2 (`k-roles.spec.ts` ya cubre un caso feliz de la v2, pero no reemplaza toda la cobertura de D). Variables nuevas para Vercel: `SITIO_URL` (recomendada) y confirmar que `RESEND_API_KEY`, `EMAIL_FROM` y `RESEND_TEMPLATE_SORTEO_BOLETA` estén cargadas antes del próximo deploy (si falta alguna, el build ahora falla a propósito). Pendiente de la cooperativa (ya reportado antes): aprobar la plantilla `ga-sorteo-boleta` en Resend y confirmar el tope de 3 reenvíos/día. No toqué las migraciones 000600/000700/000800 (ya existían) ni las apliqué a producción.
